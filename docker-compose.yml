services:
  simple_tts:
    build:
      context: .
      dockerfile: ./compose/dev/Dockerfile
    command: "uvicorn src.main:run_api --reload --factory --host 0.0.0.0"
    volumes:
      - ./src:/code/src
      - ./models:/code/models
    ports:
      - "8000:8000"
